#@data/values
---
deployment:
  args:
    - --source=ingress
    - --source=contour-httpproxy
    - --provider=rfc2136
  securityContext:
    runAsUser: 1000
    runAsGroup: 2000
    allowPrivilegeEscalation: false
