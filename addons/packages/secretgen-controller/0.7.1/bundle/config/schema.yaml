#! schema.yml

#@data/values-schema
#@schema/desc "OpenAPIv3 Schema for secret gen controller"
---
#@schema/desc "Configuration for secret gen controller"
secretgenController:
  #@schema/desc "Namespace for secret gen controller"
  namespace: secretgen-controller
  #@schema/desc "Whether to create namespace for secret gen controller if not present"
  createNamespace: false
