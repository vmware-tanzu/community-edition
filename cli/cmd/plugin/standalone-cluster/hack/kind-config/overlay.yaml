#@ load("@ytt:overlay", "overlay")
#@ load("@ytt:data", "data")

#@overlay/match by=overlay.subset({"kind":"Cluster"})
---
nodes:
#@overlay/match by=overlay.subset({"role":"control-plane"})
-
  extraPortMappings:
  #@overlay/match by=overlay.subset({"containerPort":80})
  -
    hostPort: #@ data.values.hostPort
