export const DOCKER_MC_BASIC_TAB_NAMES = ['Prerequisites', 'Cluster Settings'];

export const enum DOCKER_FIELDS {
    CLUSTER_NAME = 'clusterName',
    CNI_TYPE = 'cniType',
    CLUSTER_SERVICE_CIDR = 'clusterServiceCidr',
    CLUSTER_POD_CIDR = 'clusterPodCidr',
}
